This is the samplewise_phasediff_carriersync-write-a-block package meant as a guide to building
out-of-tree packages. To use the samplewise_phasediff_carriersync blocks, the Python namespaces
is in 'samplewise_phasediff_carriersync', which is imported as:

    import samplewise_phasediff_carriersync

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(samplewise_phasediff_carriersync)
